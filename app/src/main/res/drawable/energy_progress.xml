<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/colorPrimaryDark"
                android:centerY="1.0"
                android:angle="270" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/colorOperational"
                    android:centerColor="@color/colorIntermediate"
                    android:centerY="0.5"
                    android:endColor="@color/colorRepair"
                    android:angle="180" />
            </shape>
        </clip>
    </item>
</layer-list>